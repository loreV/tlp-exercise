server:
  servlet:
    contextPath: /api/v1
  port: 8991

springdoc:
  api-docs:
    path: /api-docs


spring:
  datasource:
    driverClassName: org.h2.Driver
    username: username
    password: password
  jpa.database-platform: org.hibernate.dialect.H2Dialect
